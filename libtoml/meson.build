# Build libtoml
libtoml_sources = [
    'toml.c',
    'toml_parse.c',
    'toml_private.c',
]

libtoml = static_library(
    'toml',
    libtoml_sources,
    include_directories : inc,
    install : false,
)
