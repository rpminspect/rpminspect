[remedy]
vendor = """
Change the string specified on the 'Vendor:' line in the spec file.
"""

buildhost = """
Make sure the SRPM is built on a host within the expected subdomain.
"""

badwords = """
Unprofessional language as defined in the configuration file was found
in the text shown.  Remove or change the offending words and rebuild.
"""

emptyrpm = """
Check to see if you eliminated a subpackage but still have the
%package and/or the %files section for it.
"""

lostpayload = """
Check to see if you eliminated a subpackage but still have the
%package and/or the %files section for it.
"""

license = """
The License tag must contain an approved license string as defined by
the distribution (e.g., GPLv2+).  If the license in question is
approved, the license database needs updating in the rpminspect-data
package.
"""

licensedb = """
Make sure the licensedb setting in the rpminspect configuration is set
to a valid licensedb file.  This is also commonly due to a missing
vendor specific rpminspect-data package on the system.
"""

unapproved_license = """
The specified license abbreviation is not listed as approved in the
license database.  The license database is specified in the rpminspect
configuration file.  Check this file and send a pull request to the
appropriate upstream project to update the database.  If the license
is listed in the database but marked unapproved, you may need to work
with the legal team regarding options for this software.
"""

invalid_boolean = """
The License tag contains SPDX license identifiers.  When SPDX
identifiers are used, the boolean joining terms must be written in all
capitable letters per the spec.  The actual SPDX identifiers are
case-insensitive.  It is only the boolean terms that must be in all
capital letters.
"""

elf_textrel = """
Ensure all object files are compiled with -fPIC.
"""

elf_execstack_missing = """
Ensure that the package is being built with the correct compiler and
compiler flags.
"""

elf_execstack_invalid = """
The data in an ELF file appears to be corrupt; ensure that packaged
ELF files are not being truncated or incorrectly modified.
"""

elf_execstack_executable = """
An ELF stack is marked as executable. Ensure that no execstack options
are being passed to the linker, and that no functions are defined on
the stack.
"""

elf_gnu_relro = """
Ensure executables are linked with with '-z relro -z now'.
"""

elf_fpic = """
Ensure all object files are compiled with -fPIC.
"""

man_errors = """
Correct the errors in the man page as reported by the libmandoc
parser.
"""

man_path = """
Correct the installation path for the man page. Man pages must be
installed in the directory beneath /usr/share/man that matches the
section number of the page.
"""

xml = """
Correct the reported errors in the XML document.
"""

desktop = """
Refer to the Desktop Entry Specification at
https://standards.freedesktop.org/desktop-entry-spec/latest/ for help
correcting the errors and warnings.
"""

disttag = """
The Release: tag in the spec file must include a '%{?dist}' string.
Please add this to the spec file per the distribution packaging
guidelines.
"""

specname = """
The spec file name does not match the expected NAME.spec format.
Rename the spec file to conform to this policy.
"""

modularity_label = """
This package is part of a module but is missing the %{modularitylabel}
header tag.  Add this as a %define in the spec file and rebuild.
"""

modularity_release = """
This package is part of a module but lacks a conformant Release tag
value.  A Release tag in a modular RPM needs to carry a substring that
is more specific than a major release dist tag (e.g., el8.9.0 rather
than el8) and must carry '+module' as a substring before that specific
dist tag.
"""

modularity_static_context = """
This build either contains a valid or invalid /data/static_context
setting.  Refer to the module rules for the product you are building
to determine what the setting should be.  The rpminspect configuration
settings also set the rules determining if the /data/static_context
setting is required, forbidden, or recommend.
"""

javabytecode = """
The Java bytecode version for one or more class files in the build was
not met for the product release.  Ensure you are using the correct JDK
for the build.
"""

changedfiles = """
File changes were found.  In most cases these are expected, but it is
a good idea to verify the changes found are deliberate.
"""

movedfiles = """
Unexpected file moves were found.  Verify these changes are correct.
If they are not, adjust the build to prevent the file moves between
builds.
"""

removedfiles = """
Unexpected file removals were found.  Verify these changes are
correct.  If they are not, adjust the build to prevent the file
removals between builds.
"""

addedfiles = """
Unexpected file additions were found.  Verify these changes are
correct.  If they are not, adjust the build to prevent the file
additions between builds.  If they are correct, update %s and send a
patch to the rpminspect data project owning that file so rpminspect
knows to expect this change.  You may also need to update the data
package or local configuration file and change the
forbidden_path_prefixes or forbidden_path_suffixes list.
"""

upstream = """
Unexpected changed source archive content. The version of the package
did not change between builds, but the source archive content
did. This may be deliberate, but needs inspection. If this change is
expected, update %s and send a patch to the project that owns that
file.
"""

ownership_defattr = """
Make sure the %%files section includes the %%defattr macro. If these
permissions are expected, update %s and send a patch to the project
that owns it.
"""

ownership_bin_owner = """
Bin path files must be owned by the bin_owner set in the rpminspect
configuration, which is usually root. If this ownership is expected,
update %s and send a patch to the project that owns it.
"""

ownership_bin_group = """
Bin path files must be owned by the bin_group set in the rpminspect
configuration, which is usually root. If this ownership is expect,
update %s and send a patch to the project that owns it.
"""

ownership_ixoth = """
Either chgrp the file to the bin_group set in the rpminspect
configuration or remove the world execute bit on the file (chmod
o-x). If this ownership is expected, update %s and send a patch to the
project that owns it.
"""

ownership_iwgrp = """
Either chgrp the file to the bin_group set in the rpminspect
configuration or remove the group write bit on the file (chmod
g-w). If this ownership is expected, update %s and send a patch to the
project that owns it.
"""

ownership_changed = """
Verify the ownership changes are expected. If not, adjust the package
build process to set correct owner and group information. If expected,
update %s and send a patch to the project that owns it.
"""

fileinfo_rule = """
rpminspect is expecting a fileinfo rule from the vendor data package
for this file. Usually this means the file carries a non-standard set
of permissions (e.g., setuid) which is a condition where rpminspect
would check the fileinfo list to ensure the package conforms to the
vendor rules. To remedy, add a fileinfo rule for this file to the
vendor data package under the appropriate product release file.
"""

shellsyntax = """
Consult the shell documentation for proper syntax.
"""

shellsyntax_gained_shell = """
The file referenced was not a known shell script in the before build
but is now a shell script in the after build.
"""

shellsyntax_bad = """
The referenced shell script is invalid. Consider debugging it with the
'-n' option on the shell to find and fix the problem.
"""

annocheck = """
See annocheck(1) for more information.
"""

annocheck_fortify_source = """
Ensure all object files are compiled with '-O2 -D_FORTIFY_SOURCE=2',
and that all appropriate headers are included (no implicit function
declarations). Symbols may also appear as unfortified if the compiler
is unable to determine the size of a buffer, which is not necessarily
an error.
"""

dsodeps = """
DT_NEEDED symbols have been added or removed.  This happens when the
build environment has different versions of the required libraries.
Sometimes this is deliberate but sometimes not.  Verify these changes
are expected.  If they are not, modify the package spec file to ensure
the build links with the correct shared libraries.
"""

filesize_grew = """
A file grew by a noticeable amount.  Ensure this change is intended.
If it is, you can adjust the filesize inspection settings in the
rpminspect.yaml file.
"""

filesize_shrank = """
A file shrank by a noticeable amount.  Ensure this change is intended.
If it is, you can adjust the filesize inspection settings in the
rpminspect.yaml file.
"""

filesize_became_not_empty = """
A previously empty file is no longer empty.  Make sure this change is
intended and fix the package spec file if necessary.
"""

filesize_became_empty = """
A previously non-empty file is now empty.  Make sure this change is
intended and fix the package space file if necessary.
"""

capabilities = """
Unexpected capabilities were found on the indicated file.  Consult
capabilities(7) and either adjust the files in the package or modify
the capabilities list in the rpminspect vendor data package.  The
security team may also be of help for this situation.  If necessary,
update %s with the changes found here and send a patch to the project
that owns the data file.
"""

kmod_parm = """
Kernel module parameters were removed between builds.  This may
present usability problems for users if module parameters were removed
in a maintenance update.
"""

kmod_deps = """
Kernel module dependencies changed between builds.  This may present
usability problems for users if module dependencies changed in a
maintenance update.
"""

kmod_alias = """
Kernel module device aliases changed between builds.  This may present
usability problems for users if module device aliases changed in a
maintenance update.
"""

arch_lost = """
An architecture present in the before build is now missing in the
after build.  This may be deliberate, but check to make sure you do
not have any unexpected ExclusiveArch lines in the spec file.
"""

arch_gain = """
A new architecture has appeared in the after build.  This may indicate
progress in the world of computing.
"""

subpackages_lost = """
A subpackage present in the before build is now missing in the after
build.  This may be deliberate, but check to make sure you have
correct syntax defining the subpackage in the spec file.
"""

subpackages_gain = """
A new subpackage has appeared in the after build.  This may indicate
progress in the world of computing.
"""

changelog = """
Make sure the spec file in the after build contains a valid %changelog
section.
"""

pathmigration = """
Files should not be installed in old directory names.  Modify the
package to install the affected file to the preferred directory.
"""

lto = """
ELF .o and .a files should not carry LTO (Link Time Optimization)
bytecode.  Make sure you have stripped LTO bytecode from those files
at install time.
"""

symlinks = """
Make sure symlinks point to a valid destination in one of the
subpackages of the build; dangling symlinks are not allowed.  If you
are comparing builds and have a non-symlink turn in to a symlink,
ensure this is deliberate.  NOTE: You cannot turn a directory in to a
symlink due to RPM limitations.
"""

symlinks_directory = """
Make sure symlinks point to a valid destination in one of the
subpackages of the build; dangling symlinks are not allowed.  If you
are comparing builds and have a non-symlink turn in to a symlink,
ensure this is deliberate.  NOTE: You cannot turn a directory in to a
symlink due to RPM limitations.  If you absolutely must do that, make
sure you include the %pretrans scriptlet for replacing a directory.
See the packaging guidelines for 'Scriptlet to replace a directory'
for more information.
"""

file_paths = """
Remove forbidden path references from the indicated line in the %files
section.  In many cases you can use RPM macros to specify path
locations.  See the RPM documentation or distribution package
maintainer guide for more information.
"""

types = """
In many cases the changing MIME type is deliberate.  Verify that the
change is intended and if necessary fix the spec file so the correct
file is included in the built package.
"""

abidiff = """
ABI changes introduced during maintenance updates can lead to problems
for users.  See the abidiff(1) documentation and the distribution ABI
policies to determine if this detected change is allowed.
"""

kmidiff = """
Kernel Module Interface introduced during maintenance updates can lead
to problems for users.  See the libabigail documentation and the
distribution KMI policy to determine if this detected change is
allowed.
"""

config = """
Changes to %config should be done carefully.  Make sure you have
installed the correct file and in the correct location.  If a package
is restructuring configuration files, make sure the package can handle
upgrading an existing package -or- honor the old file locations.
"""

doc = """
Changes found among the %doc files.  Verify these changes are intended
if the package is not a rebase.  Sometimes upstream projects rename or
move documentation files and the spec file needs to account for those
changes.
"""

patches_corrupt = """
An invalid patch file was found.  This is usually the result of
generating a collection of patches by comparing two trees.  When files
disappear that can lead to zero length patches in the resulting
collection.  Check to see if the source package has any zero length or
otherwise invalid patches and correct the problem.
"""

patches_missing_macro = """
The named patch is defined in the source RPM header (this means it has
a PatchN: definition in the spec file) but is not applied anywhere in
the spec file.  It is missing a corresponding %patch macro and the
spec file lacks the %autosetup or %autopatch macros.  You can fix this
by adding the appropriate %patch macro in the spec file (usually in
the %prep section).  The number specified with the %patch macro
corresponds to the number used to define the patch at the top of the
spec file.  So Patch47 is applied with either a '%patch 47', '%patch
-P 47', '%patch -P47', or '%patch47' macro.
"""

patches_mismatched_macro = """
The named patch is defined but is mismatched by number with the %patch
macro.  Make sure all numbered patches have corresponding %patch
macros.  For example, Patch47 needs to have either a '%patch 47',
'%patch -P 47', '%patch -P47', or '%patch47' macro.
"""

patches_unhandled_patch = """
The defined patch file is not something rpminspect can handle.  This
is likely a bug and should be reported to the upstream rpminspect
project.
"""

virus = """
ClamAV has found a virus in the named file.  This may be a false
positive, but you should manually inspect the file in question to
ensure it is clean.  This may be a problem with the ClamAV database or
detection.  If you are sure the file in question is clean, please file
a bug with rpminspect for further help.
"""

politics = """
A file with potential politically sensitive content was found in the
package.  If this file is permitted, it should be added to the
rpminspect vendor data package for the product.  Modify the %s file
and send a patch to the project that owns it.
"""

badfuncs = """
Forbidden symbols were found in an ELF file in the package.  The
configuration settings for rpminspect indicate the named symbols are
forbidden in packages.  If this is deliberate, you may want to disable
the badfuncs inspection.  If it is not deliberate, check the man pages
for the named symbols to see what API functions have replaced the
forbidden symbols.  Usually a function is marked as deprecated but
still provided in order to allow for backwards compatibility.
Whenever possible the deprecated functions should not be used.
"""

runpath = """
Either DT_RPATH or DT_RUNPATH properties were found on ELF shared
objects in this package.  The use of DT_RPATH and DT_RUNPATH is
discouraged except in certain situations.  Check to see that you are
disabling rpath during the %build stage of the spec file.  If you are
unable to do this easily, you can try using a program such as patchelf
to remove these properties from the ELF files.
"""

runpath_both = """
Both DT_RPATH and DT_RUNPATH properties were found in an ELF shared
object.  This indicates a linker error and should not happen.  ELF
objects should only carry DT_RPATH or DT_RUNPATH, never both.
"""

unicode = """
The rpminspect configuration file contains a list of forbidden Unicode
code points.  One was found in the extracted and patched source tree
or in one of the text source files in the source RPM.  Either remove
this code point or discuss the situation with the Product Security
Team to determine the correct course of action.
"""

unicode_prep_failed = """
The %prep section of the spec file could not be executed for some
reason.  This usually results from a failure in librpmbuild, which is
usually tied to archive extraction problems or the filesystem changing
while rpminspect is running.  A common cause is removal of the working
directory while the program is executing.
"""

rpmdeps_macros = """
Unexpanded RPM spec file macros were found in the noted dependency
rule.  Check the spec file for this dependency and ensure you have not
misspelled a macro or used a macro name that does not exist.
"""

rpmdeps_explicit = """
Add the indicated explicit Requires to the spec file for the named
subpackage.  Subpackages depending on shared libraries in another
subpackage must carry an explicit 'Requires: SUBPACKAGE_NAME =
%{version}-%{release}' in the spec file.
"""

rpmdeps_explicit_epoch = """
Add the indicated explicit Requires to the spec file for the named
subpackage.  Subpackages depending on shared libraries in another
subpackage must carry an explicit 'Requires: SUBPACKAGE_NAME =
%{epoch}:%{version}-%{release}' in the spec file.
"""

rpmdeps_multiple = """
Check subpackage %files sections and explicit Provides statements.
Only one subpackage should provide a given shared library.  Shared
library names are automatically added as Provides, so there is no need
to specify them in the spec file but you do need to make sure only one
subpackage is packaging up the shared library in question.
"""

rpmdeps_changed = """
A dependency listed in the before build changed to the indicated
dependency in the after build.  If this is a VERIFY result, it means
rpminspect noticed the change in what it considers a maintenance
update in a package.  An INFO result means it noticed this change, but
deems it ok because it is comparing a rebased build.
"""

rpmdeps_gained = """
A new dependency is seen in the after build that was not present in
the before build.  If this is a VERIFY result, it means rpminspect
noticed the change in what it considers a maintenance update in a
package.  An INFO result means it noticed this change, but deems it ok
because it is comparing a rebased build.
"""

rpmdeps_lost = """
A dependency seen in the before build is not seen in the after build
meaning it was removed or lost.  If this is a VERIFY result, it means
rpminspect noticed the change in what it considers a maintenance
update in a package.  An INFO result means it noticed this change, but
deems it ok because it is comparing a rebased build.
"""

rpmdeps_epoch = """
The package has an Epoch value greater than zero, but the explicit
subpackage dependencies are not consistently using it.  For the
dependency reported, the '= %{version}-%{release}' needs to change to
'= %{epoch}:%{version}-%{release}' to capture the package Epoch in the
dependency.
"""

udevrules = """
Refer to the udev documentation at
https://www.freedesktop.org/software/systemd/man/udev.html for help
correcting the errors and warnings.
"""
